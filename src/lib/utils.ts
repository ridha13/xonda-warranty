export const getToken=():string=>typeof document==="undefined"?"":document.cookie.split(";").find(c=>c.trim().startsWith("token="))?.split("=")[1]||"";export const formatDate=(d:string)=>new Date(d).toLocaleDateString("en-SA");export const statusMap:Record<string,{label:string;color:string}>={PENDING:{label:"Pending",color:"#f59e0b"},APPROVED:{label:"Approved",color:"#10b981"},REJECTED:{label:"Rejected",color:"#ef4444"},REVISION_REQUESTED:{label:"Needs Revision",color:"#8b5cf6"}};